<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baby">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterActivity" /> <!-- <activity -->
        <!-- android:name=".TabFeedingActivity" -->
        <!-- android:label="@string/title_activity_tab_feeding" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <!-- <activity android:name=".ChronometerActivity" /> -->
        <!-- <activity android:name=".NotesActivity" /> -->
        <activity android:name=".MedicationActivity" />
        <activity android:name=".SleepActivity" />
        <activity android:name=".PumpingActivity" />
        <activity android:name=".DiaperActivity" />
        <activity android:name=".SolidFeedingActivity" />
        <activity android:name=".BottleFeedingActivity" />
        <activity android:name=".BreastFeedingActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>